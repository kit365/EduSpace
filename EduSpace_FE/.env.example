# Cổng API = GATEWAY_PORT trong EduSpace_BE/.env (mặc định 8080)
VITE_API_BASE_URL=http://localhost:8080
VITE_API_PREFIX=/api/v1

# (Tuỳ chọn) Gọi trực tiếp eduspace-ai từ browser — cần CORS trên Python (KYC_AI_CORS_ORIGINS)
# Bỏ trống = chỉ dùng account-service /api/v1/accounts/me/ekyc/verify
# VITE_KYC_AI_BASE_URL=http://localhost:8000
# VITE_KYC_AI_API_KEY=dev-kyc-ai-secret
