# Python 3.10+ recommended (see Dockerfile). On 3.9, annotations use typing.Optional (not str | None).
#
# Chạy nhanh (từ thư mục eduspace-ai-service):
#   ./run-local.sh
#
# Hoặc tay:
#   python3 -m venv .venv && source .venv/bin/activate
#   python -m pip install --upgrade pip
#   pip install -r requirements.txt
#   export KYC_AI_API_KEY=dev-kyc-ai-secret
#   uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
#
# Swagger (/docs): đọc README.md — front OCR bắt buộc chọn file (không thì 422);
# không tick «Send empty value» cho back nếu không upload; OCR lần đầu có thể lâu.

# FastAPI
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.0

# OCR & CV (CPU)
numpy==1.26.4
opencv-python-headless==4.10.0.84
Pillow==10.4.0
pillow-heif==0.21.0
paddlepaddle==2.6.2
paddleocr==2.8.1

# Face verification & Liveness (LightweightDeep Learning)
# face_recognition==1.3.0 # Removed to avoid dlib/cmake dependency
onnxruntime==1.19.2
insightface==0.7.3
